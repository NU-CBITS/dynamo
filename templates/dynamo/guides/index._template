<div class="row-fluid">

  <div class="widget-header orange navbar accordion-header" id="edit-event-toggle-bar">
    <h3 id="current-guide-title">(%= (collection_name || "Guides") %)</h3>
  </div>

  <div id="guides-list-container" class="widget-content guide-view">
    <ul id="guides-list">
    (% _.each(elements, function(e) { %)
      <li class="choose_element" data-cid="(%= e.id %)">(%= e.html %)</li>
    (% }) %)
    </ul>
  </div>
</div>