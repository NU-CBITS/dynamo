<div class="item" data-cid="<%= item.cid %>">
  <div class="stream-icon orange">
    <em class="icon-calendar"></em>
  </div>

  <p class="date">(%= (new Date(item.get_field_value("start"))).toString("h:mm tt on MMM d, yyyy") %)</p><!-- <h4 id="show-event-title">(%= item.title %)</h4> -->

  <h4>(%= item.title %)</h4>

  <div class="row-fluid">
    <p style="color:black;"></p>

    <div id="show-start-end-times-container">
      From: <span id="show-event-start-time">(%= (new Date(item.get_field_value("start"))).toString("h:mm tt on MMM d, yyyy") %)</span>

      <div class="clearfix"></div>Until: <span id="show-event-end-time">(%= (new Date(item.get_field_value("end"))).toString("h:mm tt on MMM d, yyyy") %)</span>
    </div>
    <p>

    <p style="color:black;"></p>

    <div class="row-fluid">
      <div class="span6">
        Pleasure:

        <ul>
          <li id="show-predicted-pleasure">Predicted: (% print((item.get_field_value("predicted_pleasure") ? item.get_field_value("predicted_pleasure") : "-")) %)</li>

          <li id="show-predicted-pleasure">Actual: (% print((item.get_field_value("actual_pleasure") ? item.get_field_value("actual_pleasure") : "-")) %)</li>
        </ul>
      </div>

      <div class="span6">
        Accomplishment:

        <ul>
          <li id="show-predicted-accomplishment">Predicted: (% print(( item.get_field_value("predicted_accomplishment") ? item.get_field_value("predicted_accomplishment") : "-")) %)</li>

          <li id="show-predicted-accomplishment">Actual: (% print(( item.get_field_value("actual_accomplishment") ? item.get_field_value("actual_accomplishment") : "-")) %)</li>
        </ul>
      </div>
    </div>Emotion &amp; Intensity: <span id="show-emotion">(% print((item.get_field_value("emotion") ? item.get_field_value("emotion") : "-")) %)</span> <span id="show-emotion-intensity">(% print((item.get_field_value("emotion_intensity") ? item.get_field_value("emotion_intensity") : "-")) %)</span>
    <p>
  </div>
</div>